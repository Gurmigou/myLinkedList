#include <iostream>
#include <time.h>
#include "MyDataBase/TextDataBase.h"

using namespace std;

int main() {
    setlocale(LC_ALL, "ru");

//    int number = 148965486;
//    const char* name = "names123";
//    ofstream writer(path, ios_base::binary);
//
//    int size = strlen(name);
//
//    writer.write(name, size);
//    writer.write(reinterpret_cast<const char*>(&number), sizeof number);
//    const char* another = "sometext";
//    writer.write(another, strlen(another));
//
//    writer.seekp(8);
//
//    int val = 99;
//    writer.write((const char*)&val, sizeof val);
//
//    writer.close();
//
//    ifstream reader(path, ios_base::binary);
//    char* value = new char[size + 1];
//    int readNumber = 0;
////    int readAnother = 0;
//    char* readAnother = new char[strlen(another) + 1];
//
//    reader.read(value, size);
//    reader.read(reinterpret_cast<char*>(&readNumber), sizeof readNumber);
//    reader.read(readAnother, strlen(readAnother));
//
//    value[size] = '\0';
//    readAnother[strlen(another)] = '\0';
//
//    reader.close();
//    cout << value << ", " << readNumber << ", " << readAnother;

//    TextDataBase::runDataBase();




    return 0;
}
